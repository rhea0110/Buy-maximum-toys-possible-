// Amit wants to buy some toys for his sister. He has X rupees in his pocket. But he is confused to buy which toy and seeking your help.

Given an array consisting of cost of toys. Find the maximum number of toys he can buy with the amount X.

Note: He can buy only 1 quantity of a particular toy.

import java.util.*;
class Result{
  static int maxToys(int a[], int n, int sum){
	   int remainingAmount = sum;
       int count = 0;
      Arrays.sort(a);
       for (int i = 0; i < n; i++)
       {
           if (remainingAmount < a[i])
               break;
           remainingAmount -= a[i];
           count++;
       }
       return count;
  }
}

class Main
{
  public static void main(String[] args){

    int i,n, sum;
    Scanner s=new Scanner(System.in);
    sum = Integer.parseInt(s.nextLine().trim());
    n = Integer.parseInt(s.nextLine().trim());
    int a[]=new int[n];
    for(i=0;i<n;i++)
      a[i] = s.nextInt();
    System.out.println(Result.maxToys(a,n,sum));
  }
}
